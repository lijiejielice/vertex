Test Vertex AI endpoint
Spin up a Vertex AI supproted LLM model as an enpoint, send requests over.  

https://cloud.google.com/vertex-ai/docs/general/deployment

How To:
1. You need to config ".env" file to include PROJECT_ID, LOCATION, ENDPOINT_ID to consume Vertex AI and pre-created Endpoint. Endpoint in this example is deployed from Vertex Model Garden: Stable Diffusion (text to image).

2. This app uses [User Accounts (3-legged OAuth 2.0)](https://googleapis.dev/python/google-api-core/latest/auth.html#user-accounts-3-legged-oauth-2-0-with-a-refresh-token) `gcloud auth application-default login`

3. `pip install -r requirements.txt`

4. `python path/to/test_extension.py`
